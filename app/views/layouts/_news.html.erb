<div id="recent_news">
	<h3>Recent Security Articles</h3>
	<ul>
	<% for entry in FeedEntry.all(:limit => 10, :order => "published_at desc") %>
		<li><%= link_to h(entry.name), entry.url %> (<%= h(entry.published_at) %>) <br />
		<p><%= h(entry.summary) %></p>
		</li>
		<% end %>
		</ul>
</div>

